# IndexTTS-2.5 增强工程 · 待办清单

> 这份清单是本工程的**唯一进度真相**。每完成一项就把 `[ ]` 改成 `[x]`，
> 并把「验收证据」一栏填上可复核的数字（测试通过数 / 实测指标），
> 不要只写「已完成」—— 三个月后没人记得当时是怎么验的。
>
> 状态标记：`[x]` 已完成并验收 · `[~]` 进行中 · `[ ]` 未开始
> 最近更新：工作台切片交互重构（选中即试听 / 不替换主素材）+ 全音频栏下载按钮（真机验收 18/18），2026-09-22

---

## 原始需求（用户原话拆解）

| # | 需求 | 状态 |
|---|---|---|
| R1 | 优雅的可视化 WebUI，模块化、UI 清晰符合逻辑 | `[x]` 阶段 1 |
| R2 | **所有功能接口都做好 UI** | `[x]` 阶段 1（官方 `webui.py` 未改动） |
| R3 | LoRA 模型**自训练** | `[x]` 阶段 2（GPT / CFM / DPO 三目标全通，真机验收 33/33） |
| R4 | 模型推理 | `[x]` 阶段 1 |
| R5 | **所有参数的提示信息** | `[x]` 阶段 1（参数手册 Tab + 逐项 tooltip） |
| R6 | 补齐缺失的底模（镜像源优先） | `[x]` 7.89 GB 全部就位 |
| R7 | 追加：LoRA 微调的**泛化能力保护** | `[x]` `guard.py` 九道防线 |
| R8 | 追加：**拼音标注**纠音 | `[x]` 66/66 测试通过 |

---

## 阶段 1 · 可视化 UI（已交付验收）

| ID | 内容 | 状态 | 验收证据 |
|---|---|---|---|
| a1 | 拉取全部模型资源 | `[x]` | 7.89 GB，仅缺示例音频（不影响功能） |
| a2 | 建 `.venv --system-site-packages` 并补依赖 | `[x]` | 复用系统 torch 2.7.1+cu118 |
| a3 | 验证 IndexTTS2 加载 + 推理 | `[x]` | 模式 0 / 2 / 3 全部跑通 |
| a4 | `webui_app/` 模块化骨架 | `[x]` | config / params / theme / widgets / context |
| a5 | 引擎服务：懒加载 / 卸载 / 显存管理 | `[x]` | QwenEmotion 串行策略已实测 |
| a6 | Tab 语音合成：全参数 UI + 逐项提示 | `[x]` | — |
| a7 | L0 参考音频工作台 | `[x]` | 分析 / 智能切片 / 降噪归一已实测 |
| a8 | Tab 批量合成 + Tab 预设管理 | `[x]` | — |
| a9 | Tab 模型资源管理 | `[x]` | 复用 `model_fetcher` + 实时进度 |
| a10 | Tab 系统监控 + Tab 参数手册 | `[x]` | — |
| a11 | **阶段 1 验收** | `[x]` | 46/46 集成测试 + 浏览器端到端 |

**入口**：`webui_pro.py`（官方 `webui.py` 一行未动）

---

## 追加需求 · 拼音标注与泛化保护

| ID | 内容 | 状态 | 验收证据 |
|---|---|---|---|
| x1 | 拼音标注：服务层 + 合成页 UI + 手册 | `[x]` | 66/66 |
| x2 | `guard.py` 泛化保护九道防线 | `[x]` | 192/192（`tools/guard_test.py`） |
| b2r | 底座蒸馏回放集 `replay.py` | `[x]` | 已并入 GPT 探针：145/145 |

**九道防线**：底座只读快照 · 参数可配置校验 · 注入面收敛 · 权重漂移体检 ·
早停 · checkpoint 保险库 · 回放抗遗忘 · adapter 强度旋钮 · 一键回滚回放

---

## 阶段 2 · 训练体系（L0 → L3 四层全做）

### L1 · SFT LoRA

| ID | 内容 | 状态 | 验收证据 |
|---|---|---|---|
| b1 | 数据集构建 + 特征离线预提取 | `[x]` | **153/153**（`tools/features_probe.py`）；卸载后显存回落 0.04 GB |
| b2 | **GPT(T2S)** LoRA 训练器 | `[x]` | **148/148**（`tools/gpt_train_probe.py`）；val 5.7057→1.7233（降 69.8%），底座逐字节未变 |
| b3 | **CFM(S2M)** LoRA 训练器 | `[x]` | **163/163**（`tools/cfm_train_probe.py`）；val 1.3378→1.1643（降 13.0%），底座逐字节未变，续训起点 val 与 checkpoint metric 逐位一致 |
| b3a | 核对 `s2mel.pth` 键名与 CFM 结构 | `[x]` | 已确认：256 个张量全在 `estimator.` 下；`load_state_dict` missing 0 / unexpected 0；98,187,344 参数；唯一 Dropout 是 `estimator.wavenet.drop` p=0.2 |
| b3b | 写 `cfm_lora.py` | `[x]` | 1132 行：Options / 配对策略 / collate / load_base_cfm / CfmTrainer；探针 [2][3][6][7][8] 节逐项验证 |
| b3c | `tools/cfm_train_probe.py` | `[x]` | 163/163：小尺寸 DiT 闭环 + 全部陷阱回归钉（mask_content / rng_guard / dropout_off / 注入面 / '*' 展开 / 续训） |

### L2 · 偏好对齐

| ID | 内容 | 状态 | 验收证据 |
|---|---|---|---|
| b4 | 奖励打分（WER via whisper + SS via campplus） | `[x]` | **49/49**（`tools/reward_probe.py`）：whisper small 转写逐字一致、自相似=1.0、端到端满分/坏样本 reward 1.0 vs -0.01 |
| b5 | DPO 偏好对构造 + 训练器 | `[x]` | **70/70**（`tools/dpo_probe.py`）：★ln2 不变量（step-0 loss=ln2、acc=0）、loss 0.693→0.568、acc→1.0、margin 0.275、续训逐位还原、构造器编排（margin 过滤/追加/覆盖/中断） |

### L3 · 评测与落地

| ID | 内容 | 状态 | 验收证据 |
|---|---|---|---|
| b6 | 自动评测台：A/B 对比 + 指标报告 + 试听 | `[x]` | **28/28**（`tools/eval_probe.py`）：同种子公平对比、胜负表、report.json/md、试听 wav、引擎状态还原 |
| b7 | LoRA 权重合并回 `gpt.pth` / `s2mel.pth` + 推理端加载（含强度旋钮） | `[x]` | **25/25**（`tools/merge_probe.py`）：Conv1D 转置/Linear 双向合并数学逐层对账（含 α/r scaling）、红线拦截、备份、挂载/强度旋钮 |
| b8 | 训练相关 4 个 Tab 接入 UI（含泛化保护面板） | `[x]` | **20/20**（`tools/stage2_ui_probe.py`）+ build_check 11 Tab；`runner.py` 后台执行/互斥/取消 + 数据集/训练/对齐/评测部署四个 Tab |
| b9 | **阶段 2 验收**：完整训练闭环跑通 | `[x]` | **33/33**（`tools/stage2_acceptance.py` 真机 4.0 分钟）：引擎合成 28 句建集 → 特征提取 → GPT 真训（峰值 1.87GB，漂移 0.000423）→ CFM 真训（漂移 0.00319）→ DPO 偏好对真机构造（8 候选 1 对成对）→ A/B 评测 → 强度旋钮 48 层 0.7 → GPT/CFM 合并产物校验 missing/unexpected=0，全程底座复校通过 |

### 杂项

| ID | 内容 | 状态 |
|---|---|---|
| z1 | 清理临时文件（`_*.log`、`tools/_gpt_bisect.py`、`tools/_dit_timing.py`、`tools/_cfm_scan.py` 等）；**保留** `tools/_env.py`（探针公共引导，非临时文件） | `[x]` | 已删 10 个临时文件（7 根目录日志/扫描 + 3 个 tools 调试脚本），仅存 `tools/_env.py` |

---

## 已建成的代码资产

| 文件 | 作用 | 行数 |
|---|---|---|
| `webui_app/training/guard.py` | 九道防线：快照 / 校验 / 扫描注入面 / 漂移 / 早停 / 保险库 / 显存体检 | ~1515 |
| `webui_app/training/dataset.py` | 数据集与 `meta.jsonl`（含长度字段回写） | ~700 |
| `webui_app/training/features.py` | 离线特征提取（campplus / w2v-bert / codec / mel / mu_prompt / mu_target） | ~836 |
| `webui_app/training/forward.py` | **唯一**的训练前向，绕开 3 个官方陷阱 | 435 |
| `webui_app/training/trainer_base.py` | GPT / CFM 共用骨架 + `FeaturePool` + `inject_lora` | ~1286 |
| `webui_app/training/gpt_lora.py` | GPT(T2S) LoRA 训练器 | 758 |
| `webui_app/training/replay.py` | 底座蒸馏回放集（抗灾难性遗忘） | ~548 |
| `webui_app/training/runs.py` | 训练记录 / adapter 同步 / 保险库入口 | ~430 |
| `tools/features_probe.py` | 特征链路回归 | 153 项 |
| `tools/gpt_train_probe.py` | GPT 训练器回归 | 148 项 |
| `tools/guard_test.py` | 泛化保护回归 | 194 项 |
| `webui_app/training/cfm_lora.py` | CFM(S2M) LoRA 训练器 | 1132 |
| `webui_app/training/reward.py` | 奖励打分（whisper WER + campplus SS） | ~400 |
| `webui_app/training/dpo.py` | DPO 偏好对存储/构造器/训练器 | ~800 |
| `webui_app/training/evaluate.py` | A/B 评测台（复用 reward） | ~400 |
| `webui_app/training/merge.py` | LoRA 合并 + 推理端挂载/强度 | ~300 |
| `webui_app/training/runner.py` | 后台执行器（互斥/取消/进度） | ~250 |
| `webui_app/tabs/dataset_tab.py` 等 4 个 | 数据集 / 训练 / 对齐 / 评测部署 Tab | ~1600 |
| `tools/cfm_train_probe.py` | CFM 训练器回归 | 163 项 |
| `tools/reward_probe.py` | 奖励打分回归（含真模型段） | 49 项 |
| `tools/dpo_probe.py` | DPO 回归（含 ★ln2 不变量） | 70 项 |
| `tools/eval_probe.py` | 评测台回归 | 28 项 |
| `tools/merge_probe.py` | 合并/挂载回归（双向数学对账） | 25 项 |
| `tools/stage2_ui_probe.py` | UI/Runner 集成回归 | 20 项 |
| `tools/stage2_acceptance.py` | 阶段 2 真机验收闭环 | 33 项 |

---

## 已钉死的关键事实（改动前务必先看）

**架构常量**

- w2v-BERT / codes / mel 帧率 = 50 Hz / 25 Hz / 86.13 Hz（22050÷256）
- `MEL_PER_CODE = 2 × 1.72 ≈ 3.44`；1.72 是 **50Hz→86.13Hz**，不是 codes→mel
- GPT：24 层 / 1280 维 / 20 头 / 813M / `max_mel_tokens=1815` / codebook 8194
- CFM(DiT)：13 层 / 512 维 / 8 头 / **98,187,344** 参数 / L1Loss / `in_channels=80`
- 参考音频在推理时被截到 **15 s**（`_load_and_cut_audio(..., 15)`）→ 1292 mel 帧上限
- 单条音频硬上限 1815 ÷ 25Hz = **72.6 s**
- 真类在 `indextts/gpt/model_v2.py`；`model_v2_5.py` 是死代码

**六个必须绕开的官方陷阱**（全部实测复现，已在 `forward.py` / `trainer_base.py` 里落地）

1. `UnifiedVoice.forward()` 算 text_emb 时**漏了 `lang_embedding`**（:639 vs :681）
2. `UnifiedVoice.forward()` 在 bf16 下 RuntimeError（:633 `torch.zeros` 未指定 dtype）
3. `BASECFM.forward()` **不能在 `eval()` 下调**：`prompt_lens` 被传进 `mask_content`，
   触发 class_dropout → prompt_x / mu / style **全被乘 0**，val loss 变成与条件无关的常数
4. CFM 必须先 `setup_caches()`，且缓存属性住在 **`DiT.transformer`** 上（DiT 只转发）；
   `layers_emit_skip` / `layers_receive_skip` 也在这里设置，不调就 AttributeError
5. **reentrant 梯度检查点 + 全冻结底座 = 训练静默失效**（梯度恒 0）→ 必须 `use_reentrant=False`
6. `Transformer.forward` 只在 `mask is None` 时读 `causal_mask`，而 DiT 总是传 mask
   → 官方按 8192² 分配的 **67 MB causal_mask 从不被读取**，纯浪费

**四个自己踩出来的 bug（已修，均已加回归钉）**

7. `resume_from` 只恢复优化器状态、不恢复 adapter 权重 → 续训从纯底座重来且**不报错**
8. `evaluate()` 的 val 下标落到训练池上 → 「验证集」实际是训练集前几条，145 项测试全过都没发现
9. CFM 注入面预设失效：DiT 的注意力叫 **`wqkv` / `wo`**，不是 `qkv` / `out_proj`
10. `import_audio(copy=True)` 会把文件**重命名成 `<uid>.wav`** → 按原文件名做的映射永远匹配不上

**环境陷阱**

- Windows WDDM：显存溢出后**不报 OOM**，而是静默降速 20~30 倍（CFM 25 步 2.4s → 66s）
  → 所以 `vram_preflight(raise_on_short=True)` 必须硬拦
- RTX 4060 Laptop 8GB：引擎常驻 4.94~5.70 GB；训练前必须先卸载引擎
- `copy.deepcopy(CFM)` **会失败**（`weight_norm` 的 hook 存了非叶子张量）→ 需要副本时重新构造
- PowerShell 不支持 `&&`，用 `;`


---

## 阶段 3 · 工程化与发布（已完成）

| ID | 内容 | 状态 | 验收证据 |
|---|---|---|---|
| c1 | 修复被 `uv sync` 覆盖的环境，补齐缺失依赖 | `[x]` | uv venv（Python 3.11.13 + torch 2.8.0+cu128）补装 gradio/peft/pypinyin；`build_check` 11 Tab 通过 |
| c2 | `pyproject.toml` 声明真实依赖 + 作者 | `[x]` | 基础依赖加 `peft>=0.14`、`pypinyin>=0.51`；作者加 SrQingChen；`uv lock` 一致（191 包） |
| c3 | 启动器 `start.bat` | `[x]` | 自检 venv/依赖/模型/CUDA → 缺依赖可一键补装 → 启动并开浏览器；实测 7860 监听正常 |
| c4 | 作者标签与许可分层 | `[x]` | `NOTICE`（归属链 + 修改清单 + 上游 §4.1a 强制免责声明）、`LICENSE-ADDITIONS.txt`（GPL-3.0）；上游 `LICENSE`/`LICENSE_ZH.txt` 原样保留 |
| c5 | 清理测试产物并推送 GitHub | `[x]` | 保留 4 份验收报告到 `docs/verification/`；推送 **409 文件 / 39.8 MB**（无模型权重/venv/产物），https://github.com/SrQingChen/IndexTTS-2.5-Pro |
| c6 | 项目专属 README（展示 UI 与功能集成） | `[x]` | 新 `README.md`（11 Tab 截图 + 能力对照表 + 训练体系 + 验证结果 + 分层许可）；上游 README 原样存档到 `docs/README_UPSTREAM.md` |
| c7 | `tools/ui_screenshots.py` 自动截图 | `[x]` | 走 Chrome CDP（无新依赖）自动遍历 11 Tab 截图到 `assets/ui/`；**踩坑：Gradio 5 的 Tab 栏在 DOM 里有两份同名按钮**（1px 隐藏副本 + 32px 真实 tab），按文本找 `button` 必点到死元素且不报错 |

**许可分层说明**（为什么不整体换 MIT/Apache）：

- 上游 `LICENSE` 是 bilibili 自定义模型许可，§3.4(b) 要求保留原始版权声明与协议副本、
  §4.1(a) 要求下游声明免责 —— **不可替换**，替换即违约。
- 因此采用两层：Model 与上游代码 → bilibili 许可（不变）；
  SrQingChen 的**原创新增文件** → GPL-3.0（copyleft + 保留署名），
  正好满足「可自由拉取优化，但基于本项目修改需遵循协议并保留贡献」。
- 两层冲突时以上游协议为准（GPL 不延伸至 Model）。详见 `NOTICE`。

---

## 环境陷阱（新增，务必先看）

- **`uv sync` 会就地覆盖 `.venv`**。本项目原环境是
  `.venv --system-site-packages`（借系统 Python 3.10 的 torch 2.7.1+cu118）
  + venv 内装 gradio/peft/pypinyin。直接跑 `uv sync` 会把这三样一起清掉，
  表现为「启动就报 No module named 'gradio'」。
  修复：`uv sync --extra webui`（peft/pypinyin 已在基础依赖里）。
- **不要用 `uv sync --all-extras` 作为默认路径**：它还会拉
  deepspeed / flash-attn / torch_compile，需要 CUDA 工具链且非必需。
- **`.bat` 必须是纯 ASCII**。cmd.exe 用**当前代码页**逐行解析批处理文件，
  UTF-8 中文会被按 GBK 误解并连带破坏后续命令行（实测把 `echo` 行解析成
  命令、label 被截断）。中文说明写在 README / 本文件里。
- **批处理里 `%s` 会被当成环境变量吃掉**。避免在 `.bat` 内嵌 Python 字符串里
  使用 `%`（改用逗号分隔的 print 参数或 `.format()`）。

---

## 阶段 3.5 · 一键三连与推理页 LoRA 选择（已完成）

需求原话：「导入一段长音频或者大量短音频，自动执行优化、语音文字识别并对齐、
选择最合适的参数进行调整、完成训练并得到评分最优异的三个或一个 lora 模型，
整个工程的参数自动操作最好可以在这个按钮下方展示」＋「推理界面添加 lora 模型选择」。

| ID | 内容 | 状态 | 验收证据 |
|---|---|---|---|
| d1 | `training/oneclick.py` 七阶段流水线（采集切片 / 优化 / 识别对齐 / 筛选划分 / 自动调参 / 训练 / 择优） | `[x]` | 见 d6 |
| d2 | `tabs/oneclick_tab.py` + 注册第 12 个 Tab；**按钮下方实时展示全部自动操作的参数** | `[x]` | build_check 12 Tab；截图 `assets/ui/12_oneclick.png` |
| d3 | 自动调参：按数据量排候选 → **逐个真跑 preflight** → 取第一个通过的；被拒的连原因进报告 | `[x]` | 一键三连验收：4 候选逐个预检并列出裁决 |
| d4 | 推理页「LoRA 音色模型」面板：选 run / 档位 / 强度、挂载卸载、与生成联动 | `[x]` | 面板读到真机训练产物，档位全部解析成 adapter 目录 |
| d5 | `params_training.py`：38 个训练参数登记（一键三连 / 数据集 / LoRA / 优化器 / DPO / 评测），手册自动收录 | `[x]` | 一键三连探针 [10] 段 24 项 |
| d6 | `tools/oneclick_probe.py`（CPU，135 项）+ `tools/oneclick_acceptance.py`（真机 61 项） | `[x]` | **135/135** · **61/61**（3.8 分钟，报告见 `docs/verification/oneclick_report.md`） |

### 这一轮踩到 / 修掉的真实问题

1. **调参时引擎还挂着 → 全部候选被判「显存不足」**（首次真机验收就停在这里）。
   `vram_headroom()` 看到的是引擎占着的 4.9~5.7 GB，空闲只剩 1.29 GB，
   本来完全跑得动的配置（GPT 2.73 GB）被全部拒掉。修复：**特征提取一结束就卸载引擎**，
   让调参看到训练时真实的空闲显存（`set_req("none")` → `unload` → `set_req("unloaded")`）。
2. **预设的 `eval_every`（50/100/200）在小数据集上永远触发不到** → 保险库只剩轮末兜底
   存下的 1 个档位，「留 K 个档位再择优」变成没得选。修复：按总步数反推
   `eval_every = max(1, 总步数 ÷ (K+1))`（`oneclick.eval_every_for`）。
3. **`set_engine_req` 保护缺口**：一键三连必须用 `require_engine="none"` 提交
   （否则自己会把 load/unload 挡死），代价是训练期间失去「禁止加载引擎」的保护。
   新增 `TrainRunner.set_engine_req()`，流水线在阶段边界把真实需求同步回去 ——
   真机验收轮询时**同时观测到 loaded 与 unloaded**，证明双向互斥全程有效。
4. **`dataset.split_long` 并发不安全**：`load_meta`/`save_meta` 都在锁外，
   切片期间的 UI 写入会被旧快照覆盖（META_LOCK 那一类 bug）。改为
   「文件写盘在锁外、入册在锁内重读」，并加 `text` 参数与撞号重发。
5. **`collect_inputs` 单文件传 str**：Gradio 的 `gr.File` 只选一个文件时给的是 str
   而不是 list，直接迭代会逐字符拆路径。
6. **WDDM 静默降速再次实测复现**：验收跑到中途，之前被中断的一次验收留下孤儿进程
   占着 2.2 GB 显存，s2mel 从 0.88s 掉到 **65.8s（RTF 13.7）**，每句合成从 3.4s 变成 71s。
   杀掉孤儿进程后立刻恢复正常。这是本项目那条「显存溢出静默降速 20~30 倍」的又一次现场证据。

---

## 阶段 3.6 · 清理 LoRA + 模型命名 + CPU 并行 + 调试日志（已完成）

用户反馈：「把之前你测试和我自己测试生成的所有 lora 删掉」、
「自动训练前还可以输入模型名称，便于区分管理」、
「可以用到 CPU 并行加速的地方可以增加 CPU 并行加速，不过一定要确保不影响效果和稳定性」、
「没有日志吗，那你先别测试了，加上完善的 debug 日志功能」、
「默认的中文语音文字识别的那个小模型实际上很不准确，会识别出繁体字和乱的」。

| ID | 内容 | 状态 | 验收证据 |
|---|---|---|---|
| e1 | 删除全部测试生成的 LoRA（6 个 run，237 MB） | `[x]` | `training_runs/` 只剩 `base_manifest.json`；推理页下拉框回到「不使用 LoRA」 |
| e2 | **模型命名**：`OneClickOptions.model_name` + 单目标原名 / 多目标加 `_arch` 后缀 + 开工前冲突拦截 | `[x]` | oneclick_probe [12] 段；冲突在 ingest 之前就报错 |
| e3 | **CPU 并行**：新增 `training/parallel.py`，只并行音频体检与增强 | `[x]` | oneclick_probe [11] 段：**1 线程 vs 4 线程输出逐字节一致**（8 条 sha256 全等） |
| e4 | **调试日志**：新增 `logging_setup.py`，落盘 + error.log + 环形缓冲 + 全局兜底 + `@ui_guard` | `[x]` | `tools/logging_probe.py` **66/66** |
| e5 | ASR 升级：默认 `small` → **`medium`**，并加简体 `initial_prompt` 修繁体字 | `[x]` | 实测 5/5 条繁体被纠正（西蓮→西莲、烏鳩→乌纠） |
| e6 | 识别前后**显式加载/卸载**并将释放的显存写进报告 | `[x]` | 报告含 `vram_before/after/freed_gb` 与「已卸载」结论 |
| e7 | 择优阶段**复用一个打分器**（原来每候选都加载/卸载一遍 whisper） | `[x]` | `run_eval` 改为「谁创建谁卸载」；6 候选省下 5 次 1.6 GB 模型来回 |

### 这一轮的关键发现与修复

1. **`noisereduce` 根本没装 → 降噪一直是静默跳过**。`audio_lab._denoise` 捕获
   ImportError 后原样返回，界面却写着「已降噪」。真机验收里「平均体检分 94.0 → 94.0」
   纹丝不动就是这个原因（当时误判为「音频本来就干净」）。
   修复：加 `noisereduce_available()` / `denoise_note()` 能力探测，把「已跳过」
   明确写进参数面板与阶段结果，并给出安装命令。**尚未装包**（避免改 pyproject 与锁文件），
   所以当前仍会跳过 —— 但现在是**可见的**跳过。
2. **`initial_prompt` 是修繁体字的正确手段**：whisper 在 `language=zh` 下本来就倾向
   输出繁体。加一句简体提示词后实测 5/5 条全部转为简体，不换模型、零成本。
   单靠换大模型解决不了这个问题。
3. **`PeftModel` 挂载失败会留下「注进底座但没包装」的残留** —— 之后每次挂载都在
   PeftModel 上再包一层、持续报错直到引擎重载，正是用户描述的「偶现后持续报错」。
   修复：`attach_lora` 判断「是否已挂着」改为**模块真实状态 ∪ 标签**，
   失败后主动 `detach_lora` 清理；`detach_lora` 遇到状态脱节会记 warning。
   另加 `engine.lora_status()` 让面板显示真实包装状态而非标签。
4. **`get_logger` 的惰性初始化会抢跑入口配置**：它若先装好 handler，
   入口的 `setup(level=..., quiet=..., log_dir=...)` 就只会改级别 ——
   `--quiet` / `--log-dir` 全部失效。修复：惰性路径只挂 `NullHandler`，
   真正的配置只有 `setup()` 能做。
5. **`merge.set_scale` 硬依赖引擎私有方法**，把 merge_probe 的桩引擎打挂了。
   改成 `getattr` 探测：桩引擎没有该方法时保持原行为。
6. **Windows 上打开的日志句柄会导致目录删不掉** —— 为此加了
   `logging_setup.shutdown()`（探针清理临时目录前必须调用）。

### 已知未做（有意留下的）

- **`noisereduce` 未加进 pyproject**：加依赖要重刷 `uv.lock`，在网络上不稳的环境里
  风险高于收益。当前策略是「能力探测 + 明确告知 + 给出一条安装命令」。
- **LoRA 报错的复现验证**：用户明确要求先补日志、暂不测试。上面第 3 条是**代码层面
  的根因修复**，下次触发时 `error.log` 会给出确凿证据（挂载请求 / 挂载前状态 /
  失败堆栈 / 失败后清理结果）。

---

## 阶段 3.7 · 长音频切片的可观测性与内存修复（已完成）

用户反馈：「对于长音频的自动切片可能会卡死，或者说没有连续日志打印。最近一次一键三连
在切片时卡很久然后停止停了很久，但是停止后显示在优化阶段停止」。

**日志证据**（这次的日志系统派上用场了）：

```
11:12:30.961 | INFO  | train-oneclick | 后台任务开始 kind=oneclick · 1 个文件
11:17:33.027 | ERROR | train-oneclick | 后台任务失败 · 302.1s · 用户在优化阶段停止
（任务内部记的最后一个阶段：未知）
```

5 分钟里**中间一条记录都没有** —— 三个叠加的问题：

| # | 问题 | 后果 |
|---|---|---|
| 1 | `_frames_db` 一次性物化「帧数 × 窗长」索引矩阵 | 1 小时 48 kHz 音频光索引就 ~3.5 GB，加切片与 float64 转换峰值 ~**8.6 GB** → 内存颠簸，看着像卡死 |
| 2 | 切片链路全程没有进度回调 | 界面只有进入切片前的一行，中间完全静默，无法区分「在算」与「卡死」 |
| 3 | `should_stop` 没传进 `split_long`，且循环结束后没再查一次 | 停止要等整条切完才生效；而且停止被归因到**下一个阶段**（所以显示「在优化阶段停止」） |

| ID | 内容 | 状态 | 验收证据 |
|---|---|---|---|
| f1 | `_frames_db` 改分块计算（峰值与时长无关） | `[x]` | oneclick_probe [13]：与稠密实现**逐位相同**（max\|diff\|=0.0）；量化 3456 MB → 40 MB |
| f2 | `find_segments` / `split_long` 全程报进度 | `[x]` | 实测 397 秒音频：解码 / 帧能量 / 387 个窗口 / 34 片，最长间隔 0.81 秒 |
| f3 | `should_stop` 打通到解码、帧能量、窗口搜索、逐片写出 | `[x]` | 探针：立即停止时 0.002s 返回（完整 0.057s）；`split_long` 返回 `stopped=True` |
| f4 | 阶段归属修正：切片中途停止由采集阶段认领 | `[x]` | 探针：`stage_ingest` 返回 `stopped=True`（此前会被算成正常完成） |
| f5 | 阶段转换与进度写进**文件日志**；12 秒无进度则打心跳 | `[x]` | 探针：阶段开始/结束进中央日志；心跳机制在静默时报出「仍在进行」 |
| f6 | 时长判定改 `probe_duration`（只读文件头） | `[x]` | 探针：把 `AL.analyze` 打桩成报错，`_durations` 仍正常 |
| f7 | 超长音频的滑窗步长自适应（`时长÷1800`，下限 1 秒） | `[x]` | 把 1 小时音频的评分次数从 3600 收到约 1800 |

**没做的**（有意）：没有做流式解码 —— `librosa.load` 仍会整段载入内存
（1 小时 48 kHz ≈ 690 MB）。改成流式要动 `find_segments` 的整体结构，
风险大于收益；当前瓶颈（帧能量的 8.6 GB）已经消除，剩下的用进度与心跳兜住。

---

## 阶段 3.8 · 识别阶段的显存争用 + 切片的重复解码（已完成）

用户反馈：「一个识别好久好久，你看看是因为之前哪里的模型没能卸载吗？……或者说
可以在任何模型加载前做一个小小的内存显存清理？」

**日志证据**（一眼就能看出问题）：

```
11:41:09 | engine-autoload | loaded · 占用显存 4.94 GB     ← 开机自动加载后一直驻留
11:47:19 | 阶段完成 · 采集与切片 · 用时 282.7s · 切成 109 片
11:47:31 | 阶段开始 · 识别与对齐 · whisper medium 逐条转写
11:47:47 → 11:51:47 | 心跳：「已静默 XXX 秒 · 最后进度：[识别] 转写 1/110」
```

两个独立的问题：

| # | 问题 | 后果 |
|---|---|---|
| 1 | 流水线从不主动卸引擎 —— 而采集/优化/识别三个阶段**一个 GPU 模型都不需要** | 引擎 4.94 GB + whisper medium 1.6 GB > 8 GB 额定 → 溢出到共享内存 → 转写静默降速 20~30 倍（第一条音频卡了 4 分钟以上） |
| 2 | `extract_segment` 每裁一片就把**整个源文件**重新解码一遍 | 109 片 = 110 次全量解码；实测 598 秒素材切 109 片，仅解码就 ~163 秒（切片总耗时 282.7s 主要就是这个） |
| 3 | 进度行只写 DEBUG 级 | 常规 INFO 级别下切片那 282 秒里日志一条都不刷，只能靠心跳看出还活着 |

| ID | 内容 | 状态 | 验收证据 |
|---|---|---|---|
| g1 | 新增 `guard.free_vram()`（gc → empty_cache → ipc_collect，报告前后空闲量） | `[x]` | 探针 [14]：契约与无 CUDA 安全 |
| g2 | 新增 `oneclick.ensure_engine_off()`：先放开自己的引擎要求再卸载 | `[x]` | 探针 [14]：假引擎被真的卸载、`engine_set_req("none")`、失败不抛 |
| g3 | 流水线起点 + 识别前 + 特征提取前 + 择优前都做「卸/清」 | `[x]` | 日志会打印「引擎状态：已加载 → 已卸载=True · 空闲显存 X GB」 |
| g4 | whisper 加载前清显存，并记录加载耗时与加载后空闲量 | `[x]` | `ix.reward.whisper` 日志 |
| g5 | `find_segments` / `extract_segment` 支持传入已解码波形 | `[x]` | 探针 [14]：传 y/sr 时 `librosa.load` 调用数 = 0 |
| g6 | `split_long` 只解码源文件一次 | `[x]` | 探针 [14]：源文件解码 1 次（修复前 1+片数）；实测 109 片 163s → **2.2s** |
| g7 | 进度行限流升到 INFO（最多每 5 秒一条） | `[x]` | 常规级别下切片阶段也能看到连续进度，且不刷屏 |

**为什么之前的真机验收没发现**：验收脚本在提交任务前**显式卸载了引擎**，所以
识别阶段天然是独占显存的；而真实使用里引擎是开机自动加载的。这类「只在真实
使用路径上出现」的问题，正是要把日志做扎实的理由 —— 这次是两个小时内的第二条
日志直接把问题指出来了。

---

## 阶段 3.9 · 识别卡死的真正根因：转了不该转的东西（已完成）

上一轮修了「引擎没卸」与「切片重复解码」之后**仍然卡在识别**。继续查日志：

```
12:12:08 | 引擎状态：已加载 → 已卸载=True · 空闲显存 6.93 GB   ← 上一轮的修复生效
12:12:15 | whisper-medium 就绪 · 加载 7.0s · 之后空闲 2.65 GB
12:12:15 → 12:14:48 | 心跳：已静默 159 秒 · 最后进度：[识别] 转写 1/110 · utt_00001
```

`转写 1/110` 卡了 159 秒 —— 110 = 109 片切片 **+ 1 条原件**。查数据集：

```
utt_00001 时长=1738.5s 状态=no_text    ← 29 分钟的长原件，排在转写队列第一位
utt_00002 时长=   9.7s ...
```

**真正的根因**：`stage_asr` 把「所有没有文本的样本」排队转写，包括那条 29 分钟的
原件。它超过训练上限 20 秒、永远不可能参与训练（最终判 `too_long` 被筛掉），
转写它纯属浪费 —— 而 whisper 转 29 分钟音频要按 30 秒窗切 58 段，第一条就几分钟。

而且**按 `status` 过滤拦不住**：体检逻辑里「无文本」优先于「过长」，
这条原件的状态是 `no_text`，看起来和普通待转写样本一模一样。

| ID | 内容 | 状态 | 验收证据 |
|---|---|---|---|
| h1 | 识别只转写可训练样本（1~20 秒），原件跳过并回报 | `[x]` | 探针 [15]：stub 掉 whisper 后统计调用次数 —— 只转 12 片、原件 0 次；报告含跳过 id |
| h2 | 原件加标注「长音频原件（N 片的来源，不参与训练）」 | `[x]` | 探针 [15] |
| h3 | `WHISPER_SIZES` 改为**实测 fp32** 值（medium 4.28 GB） | `[x]` | 实测 base 0.42 / small 1.46 / medium 4.28 GB；探针断言数值与单调性 |
| h4 | 告警文案改成实测值 + 说明 fp32-only | `[x]` | `RewardOptions.validate()` 输出 |
| h5 | `slice_max_pieces` 默认 60 → **120** | `[x]` | 探针 [15]：半小时素材按 10~12 秒一片约 100 来条，正好够训 |
| h6 | `slice_over_sec` 不得高于训练上限（否则那段时长被静默丢弃） | `[x]` | 探针 [15]：=60 报错、=20 通过；UI 滑块上限收到 20 |

**为什么这张表错了这么久**：`WHISPER_SIZES` 最早是按 fp16 估的（medium 1.6 GB），
而 `openai-whisper` 的 `load_model()` 根本没有 dtype 参数、一律 fp32。实测
allocated 2.85 / reserved 4.28 GB，**低估 2.7 倍** —— 于是显存规划与告警全都失准。
顺带验证了「转 fp16 省显存」这条路走不通：`.half()` 会因 whisper 的 LayerNorm
强制 `x.float()` 而抛 `expected scalar type Float but found Half`（先测后改，没白费）。

真正的教训：**容量规划里的每个数字都应该是量出来的**。这张表如果一开始就实测，
「引擎 4.94 + whisper 4.28 > 8.19」在第一次设计时就能看出来。

---

## 阶段 3.10 · 退化文本：446 个「哈」把择优拖了近一小时（已完成）

用户反馈：「择优第四轮里的『哈哈哈哈』很奇怪……这一步卡了超级久，我不得不终止了」，
并建议加个过滤：「如果有一条全是重复一个字或者完全是乱码就剔除这一条」。

**日志证据**（每轮择优的那条样本都要 8~11 分钟，其余样本只要 15~20 秒）：

| 轮次 | 4/5 开始 | 5/5 开始 | 该条耗时 |
|---|---|---|---|
| 1 | 12:50:30 | 12:57:50 | 7 分 20 秒 |
| 2 | 12:58:52 | 13:09:43 | 10 分 51 秒 |
| 3 | 13:10:45 | 13:20:56 | 10 分 11 秒 |
| 4 | 13:21:51 | 13:30:23 | 8 分 32 秒 |

查出元凶：数据集里 `utt_00005` 是 **11.4 秒**的音频，文本是 **446 个「哈」**
（不同字符数 = 1，占比 100%）。它是 whisper 在笑声片段上的幻听，随后：

- 成了**训练文本** → 等于教模型「输出一长串同一个字」；
- 成了**评测文本** → 446 字触发低显存自动分块（每块 ≤40 字）= **12 块**，
  每块都可能陷入重复循环撞上 `max_mel_tokens`，加上对长音频打分，
  一条样本 8~11 分钟；`run_eval` 又固定取 `items[:n]` 的前几条，
  这条永远在名单里 → 6 个候选 × ~9 分钟。

| ID | 内容 | 状态 | 验收证据 |
|---|---|---|---|
| i1 | 新增 `degenerate_text_reason()`：单字重复 / 单字占比 ≥50% / 连续 6+ 重复 / 重复度过高 / 幻听套话 / 有效字符不足 | `[x]` | 探针 [16]：12 个用例，含「不该误伤」的反例 |
| i2 | 识别阶段丢弃退化转写（text 留空、asr_text 留档、note 记原因） | `[x]` | 探针 [16]：stub whisper 返回 446 个「哈」→ degenerate=1，该条 text 为空 |
| i3 | 筛选阶段第二道防线（连别人给的、早期数据集里的也拦得住） | `[x]` | 探针 [16]：ASR 标记的按标记判，其余按内容判 |
| i4 | 报告里说明丢弃了哪几条、为什么 | `[x]` | 阶段 info + `rep.add_note` |
| i5 | 择优改用 `pick="short"`（优先短文本，所有候选同一批样本） | `[x]` | 探针 [16]；评测页默认仍是 `first`，行为不变 |
| i6 | 择优的合成预算收到 600 帧（≈24 秒），作为重复循环的刹车 | `[x]` | 代码 + 注释；短样本本来用不到 |
| i7 | 引擎加载后再清一次显存（给打分模型腾余量） | `[x]` | 日志多一条「显存清理（引擎加载后…）」 |

**对已有数据集的影响**（扫描全部 oneclick 数据集）：

| 数据集 | 退化条数 | 例子 |
|---|---|---|
| `oneclick_20260915-1240`（卡住那次） | 4 | 446 个「哈」；「读书时的」重复 30 遍；190 个「呵」/218 字 ×2 |
| `oneclick_20260914-2239`（用户自己的） | 1 | 111 个「呃」 |

都是同一类现象：**whisper 在含笑声/噪声的片段上幻听**。下次跑一键三连它们会被
自动丢弃；已有数据集重跑「筛选」阶段也会被剔除。

**判据的克制**：只打明显退化的形态。`哈哈,工作狂,我就知道你会这么说。`
这种正常句子（只是含语气词）**不会**被误伤 —— 探针里用数据集里的真实样本做了反例。

---

## 阶段 3.11 · 音频工作台：「设为主素材」失灵（已完成）

用户反馈：「导入了一条长音频，自动切分选取合适的片段后，设为主素材的功能似乎并没有
生效，也没法存到本地也没法直接用来调优并保存到库里，应该有日志？」

**先补日志**：这个 Tab 之前**没有任何埋点** —— 用户那两次操作（14:28 / 14:31）
在日志里一条都没有，只能从产出文件反推。现已给体检 / 扫描 / 导出 / 设为主素材 /
增强 / 入库 / 音色库管理全部加上 `@ui_guard` 与 INFO 日志。

**从产出文件定位到根因**：

```
outputs/lab/爱弥斯_seg0_1789453703.wav               1,058,444 字节   ← 正常片段（12 秒）
outputs/lab/爱弥斯_seg0_1789453703_seg0_1789453727.wav       44 字节   ← 空 wav（只有文件头）
```

第二个文件名是「**第一次导出的片段**又切了一次」。链路：

1. 第一次导出成功，同时把 `src_audio` 换成了这个 **12 秒**的片段；
2. 但 `seg_state` 里的候选片段还是**相对原始长音频**的偏移（如 43.0~55.0 秒）；
3. 再点一次 → 按 43 秒去切一个 12 秒的文件 → 越界 → 切出 **0 个采样点**；
4. `save_audio` 照样写出一个 44 字节的 wav（能打开、没有声音）；
5. 后续体检判「音频为空」→ 增强、入库全失败；
6. 而 `on_seg_as_src` **无论成败**都返回「已把切片设为主要处理对象」——
   所以用户看到的就是「没生效」。

| ID | 内容 | 状态 | 验收证据 |
|---|---|---|---|
| j1 | `extract_segment` 超出音频长度（超取整容差）**直接报错**，并说明「秒数属于另一个音频，请重新扫描」 | `[x]` | lab_probe [3]：三种越界都被拦住并给出可操作提示；取整级溢出仍放过 |
| j2 | `save_audio` 拒绝写出 0 采样点 | `[x]` | lab_probe [4]：抛 ValueError 且不留文件 |
| j3 | 换主素材时候选片段自动作废（`src_audio.change` → 清空 state 与下拉框） | `[x]` | 代码 + `seg_source` 记录扫描来源 |
| j4 | 导出**一律针对扫描时那个音频**（不是当前 src_audio） | `[x]` | `_export_segment` 优先用 `seg_source` |
| j5 | 「设为主素材」导出后**顺手体检**并把报告显示出来，可立刻做增强/入库 | `[x]` | 失败时明确报错（不再谎报成功），成功时刷新报告 |
| j6 | 体检不过的音频不许进音色库，并清掉已复制的文件 | `[x]` | lab_probe [5]：空音频/不存在都拒绝，库里无残留 |
| j7 | 工作台全链路加日志埋点（9 处 `ui_guard` + 关键 INFO） | `[x]` | 下次同类问题可从日志直接定位 |
| j8 | 新增 `tools/lab_probe.py`（36 项） | `[x]` | 覆盖候选范围、越界、空音频、入库校验、正常链路 |

**顺带被 `ui_output_check` 抓到的一个自伤**：修 `on_seg_as_src` 时我给失败分支只写了
3 个返回值、而 outputs 声明是 6 个，Gradio 会在运行时抛
「didn't return enough output values」。这个静态检查工具（阶段 3.5 为同类 bug 建的）
当场拦下，已补齐。

**另一件值得记的事**：这次日志里看到用户 14:22 的一键三连**成功跑完了**
（968 秒 ≈ 16 分钟，产出 `爱弥斯_cfm/ckpt-e002-s000036`）—— 说明前几轮的修复
（引擎卸载 / 只解码一次 / 不转写不可训练样本）都生效了。

---

## 阶段 3.12 · LoRA 挂载报错 + 音频处理升级（已完成）

用户反馈两件事：「切换加载 lora 的时候报了一些错」；
「合成出的声音不是很干净有点哑……人声里高频部分是被裁掉了吗？」

### 一、LoRA 报错：`ModuleDict` 没有 `.get()`

```
AttributeError: 'ModuleDict' object has no attribute 'get'
  File "webui_app/training/merge.py", line 93, in set_scale
    else engine.tts.s2mel.models.get("cfm"))
```

`s2mel.models` 是 `torch.nn.ModuleDict`（不是 `dict` 子类），**挂载 CFM adapter
且强度 ≠ 1.0** 时必炸；GPT 那一支走 `getattr(tts, "gpt")` 所以一直没暴露。
6 处同类写法（deploy_tab / synthesize / merge / evaluate×3）全部改为
`guard.lora_target_module()`，对 dict 与 ModuleDict 都安全。

**为什么测试没发现**：`merge_probe` 的替身用的是**普通 dict**，`.get()` 在那里合法。
替身比真实对象宽松 → 测试反而掩护了 bug。探针已改用真实的 `nn.ModuleDict`，
并把「空 ModuleDict / 缺 key / ModuleDict 取子模块不抛」都钉成断言（25 → 29 项）。

### 二、音频「发闷」的诊断（全部实测，不猜）

| 检查项 | 结果 |
|---|---|
| 训练音频质量 | SNR 中位 **54.9 dB**（最低 34.9）、体检分中位 85 —— 干净，不是噪声问题 |
| 增强链是否损失高频 | **不损失**：处理前后频段能量完全一致，唯一变化是重采样到模型要求的 22050 Hz |
| CFM LoRA 是否让声音变闷 | **反而更亮**：8~11 kHz 5.27% → 5.91%，谱质心 3304 → 3562 Hz |
| 真正的上限 | **模型输出固定 22050 Hz（Nyquist 11 kHz）** —— 上游设计，改不了 |

结论：**不是处理链裁掉了高频**。但实测也确认了两个可以改进的地方，都已落地。

| ID | 内容 | 状态 | 验收证据 |
|---|---|---|---|
| k1 | **分频段降噪**：8 kHz 以上保留原始信号（`DENOISE_KEEP_HIGH_HZ`） | `[x]` | lab_probe：降噪后 8~11 kHz 能量 40.88% → 78.74%（细节保住） |
| k2 | **不再谎报降噪**：缺 noisereduce 时步骤写明「已跳过」 | `[x]` | lab_probe：monkeypatch 探测为 False → 步骤文案正确切换 |
| k3 | 装上 noisereduce（阿里镜像）+ `uv sync --extra denoise` | `[x]` | pyproject 增加 extra，`uv lock` 已刷新（192 包） |
| k4 | **前置高通 60 Hz**（去隆隆声/直流） | `[x]` | lab_probe：0-60 Hz 能量 73.53% → 0.96%，语音频段不受影响 |
| k5 | **TPDF 抖动**（16-bit 写入前，固定种子） | `[x]` | lab_probe：确定性 + 幅度 ≤1 LSB |
| k6 | **后置「输出提亮」**：presence + 谐波激励 + 峰值对齐 | `[x]` | lab_probe：中性档质心 3336→3356（不动），presence +3 → 3898，+激励 → 4212 |
| k7 | 接进合成页（默认开启温和档，另存 `*_polish.wav`） | `[x]` | build_check / ui_output_check 通过；端到端实测质心 3562 → 3946 Hz |
| k8 | 5 个新参数进手册（新分组「输出后处理（提亮）」） | `[x]` | 手册渲染通过，参数总数 83 |

**自己踩到并修掉的一个坑**：激励最初用 `preemphasis`/`deemphasis` 那一对实现，
但 `deemphasis` 是 `1/(1-0.95z⁻¹)`，直流增益 20 倍 —— 作用在**未预加重的主信号**上
会把低频整体抬起来。实测谱质心从 3355 Hz **掉到 788 Hz**、99.98% 能量挤进 0-4 kHz，
声音直接闷掉。改成真正的 butter 高通滤波后正常（激励只叠加 9 kHz 以上）。
探针里加了「激励不得让谱质心大幅下降」这条断言。

**没做也做不到的**：真实的 11 kHz 以上信息。模型不生成，任何后处理都变不出来 ——
后处理只是听感补偿（EQ + 自身谐波延展），这一点在手册与 README 里都写明了。

---

## 阶段 3.13 · 「🧹 清理」页：产物盘点 + 确认弹窗 + 安全删除（已完成）

用户需求：展示**除底模与项目之外**的全部产物（一键三连 / 自训练 / 工作台 /
合成产生的一切），提供删除按钮，且删除必须过确认弹窗防误操作。

### 设计

- **服务层** `services/artifacts.py`：`scan()` 盘点 11 类产物（一键三连评选 /
  A/B 评测 / 训练运行 / LoRA 导出 / 训练中间文件 / 数据集 / 音色库条目 /
  工作台切片 / 合成音频 / 批量任务 / 缓存），每条带体积、文件数、时间、
  来源（从 `run.json.dataset` 与数据集名前缀推断「一键三连 / 手动」）与保护标记；
  `delete_items()` 按 key 删除 —— key 只当**查找凭证**，服务端按 key 重新定位
  磁盘路径，绝不信任客户端传来的路径。
- **界面层** `tabs/cleanup_tab.py`：`@gr.render` 按扫描结果动态重绘条目行
  （每行一个 🗑 按钮，单类超 60 行折叠提示走「清空此类」）；确认弹窗第一次
  用上 theme.py 里预留多年的 `ix-modal` 覆盖层样式（`gr.Group(visible=)` 实现，
  Gradio 5.45 还没有 `gr.Dialog`）。

### 安全模型（删除是永久的，宁可保守）

| 防线 | 实现 |
|---|---|
| 白名单根目录 | 只有 artifacts.py 登记的根之下的**子项**可删，根本身永不删 |
| 永不列出 | `checkpoints/`、项目代码、`outputs/presets/`、`outputs/logs/`、`training_runs/base_manifest.json` |
| 双重路径校验 | `run_dir`/`dir_of` 自带的名称净化 + `_is_under()` realpath 包含性复核（纵深防御） |
| 运行中锁定 | `status=running` 或 runner 正在训练的 run：按钮置灰 + 服务端拒绝 |
| 正规删除通道 | 数据集 / 训练 run / 音色分别走 `DS.delete` / `runs.delete_run` / `voice_bank.remove`，索引同步更新 |
| 确认弹窗 | 弹窗列出每项路径与体积，明示「永久删除不进回收站」 |
| 幂等语义 | 扫描后已被别处删除的项记 skip 不算失败（目标是目录变干净） |

### 验收

| ID | 内容 | 状态 | 验收证据 |
|---|---|---|---|
| c1 | `services/artifacts.py` 扫描 + 删除 + 安全模型 | `[x]` | `tools/cleanup_probe.py` **18/18**（11 类发现 / 三条专用通道 / 越权 9 种伪造 key 全拒 / running 锁定 / 零残留） |
| c2 | `tabs/cleanup_tab.py` 动态行 + 确认弹窗 | `[x]` | build_check **13 Tab 全渲染**；CDP 真浏览器点 🗑 → 弹窗明细表 → 取消关闭，DOM 断言全过；截图 `assets/ui/13_cleanup.png` |
| c3 | 注册 `TAB_SPECS`（系统与手册之间）+ 入口注释 | `[x]` | `tools/build_check.py` ✅（870 组件 / 168 依赖） |
| c4 | 截图工具支持新页签与单页验收 | `[x]` | `--only 清理` 只出新图不动旧图 |
| c5 | README：13 页 / 能力对照 / §7.10 / FAQ Q15 / 测试表 | `[x]` | 断言总数 1350 → **1368** |

**一个交互坑**：`@gr.render` 里的行按钮每次重绘都会重建事件监听，Gradio 5.45
对此的原生支持没问题；真正要注意的是**行按钮的回调必须以默认参数捕获当行的
数据**（`def _one(item=it)`），闭包直接引用循环变量会全部指向最后一行 ——
这是 Python 老坑，在动态渲染里更隐蔽。

---

## 阶段 3.14 · 合成页参数记忆 + 配置档（已完成）

用户需求：合成页参数默认不记忆不保留；希望改动后默认保留、重启后恢复上次的
参数，也能单独存成配置档。

### 设计

- **服务层** `services/synth_state.py`：`outputs/state/synthesis_state.json`
  原子落盘；上传的参考音频是 Gradio 临时文件（进程退出即没），save 时把
  临时目录的音频**复制**进 state（固定两个文件名滚动覆盖），音色库 / examples /
  presets 里的稳定路径原样引用。`sanitize()` 对磁盘来的值做类型纠正 + 范围
  钳制 + 白名单过滤 —— 手改坏 json 只会丢字段，不会连累界面启动。
- **恢复走「渲染时初始值」**而不是页面加载事件回填：render() 每进程只跑一次，
  把上次的值直接塞进控件构造参数，首屏 payload 即正确，无闪烁、无事件竞态、
  不依赖 Tab.select 首屏是否触发（Gradio 对此无保证）。选择类值（语言 /
  音色 / LoRA run / 档位）恢复前校验「仍在合法集合内」。
- **保存走 `gr.on` 单依赖挂 35 个触发器**：任何控件 change 都整表落盘，
  `show_progress="hidden"` 防止高频闪加载动画。不记忆：合成文本（内容非
  配置）、读音纠正工具区、QwenEmotion 设备偏好。
- **配置档复用官方预设系统**（`outputs/presets/`，与「💾 预设」页及官方
  webui.py 完全互通）：`live_to_preset_data()` 把实时值映射成官方 19 字段
  格式；删除用两步确认（第一次布防第二次真删）。

### 踩到的坑（真机验收 + 用户实测逼出来的四个修复）

1. **Gradio 程序化设值也触发 change**（gr.update 回填 → 目标控件的 .change
   照常 fire）。后果一：「清除记忆」重置 34 个控件的回声会立刻把默认值写回，
   看起来像「清除不掉」→ 加 2 秒落盘抑制窗口。后果二：程序化设值的回调
   （音色库选择 / 快速档 / 随机向量 / 示例 / 配置档应用）不需要手动补
   `_persist_patch`，但补上无害且让音频路径的落盘时机更确定 —— 保留了。
2. **重启后立刻「存为配置档」会存成默认值**：控件恢复了上次参数，但共享
   快照 `syn_live_values` 要等第一次 change 才有值 → render 期就用恢复值
   播种共享快照。
3. **「记住参数改动」关掉后重启自己变回开**：关闭路径完全不落盘导致开关
   状态丢失 → 改为开关从开到关的**边沿**落盘一次（之后不再写）。
4. **验收脚本被强杀会留垃圾**：`e2e记忆验收_*` 配置档残留在用户的下拉里
   → finally 清理用通配；用户实测第一眼看到的就是这个垃圾档。

### 验收

| ID | 内容 | 状态 | 验收证据 |
|---|---|---|---|
| m1 | `services/synth_state.py` 落盘 / 恢复 / 音频副本 / 官方格式映射 | `[x]` | `tools/synth_state_probe.py` **30/30**（备份真实记忆，测完恢复） |
| m2 | 合成页接入：渲染恢复 + gr.on 落盘 + 配置档面板 + 清除恢复默认 | `[x]` | build_check 13 Tab ✅；ui_output_check ✅（所有分支返回值数量匹配） |
| m3 | 真机端到端：自起服务器 + 无头浏览器 + **杀进程重启三轮** | `[x]` | `tools/mem_acceptance.py` **26/26**（改动→落盘→重启逐位恢复→配置档存/选/两步删→清除回默认→关闭跨重启保持） |

**一个实现细节**：恢复情感模式时不只恢复下拉框的值，**各分组的初始可见性**
（情感参考音频 / 8 维向量 / 描述文本 / 权重 / 随机）也按恢复的模式设置 ——
否则恢复了模式 2 但向量面板不展开，用户会以为恢复失败。

---

## 阶段 3.15 · 工作台切片交互重构 + 全音频栏下载按钮（已完成）

用户反馈两件事：①「长音频切分后列出所有候选，选中候选可以直接听候选，
但现在两种操作都直接替换原音频了」；②「所有音频栏（输入/输出）都没有
下载另存功能」。

### 一、切片交互重构

旧逻辑的三宗罪：选中候选只切换按钮可用态（不能试听）；「导出该片段」把
导出结果写回主素材（还会连锁触发 src_audio.change 把候选列表整个作废，
想听下一个候选只能重来）；「开始体检」还把整段源音频塞进片段预览播放器。

新交互：

| 操作 | 行为 |
|---|---|
| 扫描候选片段 | 列出全部候选，**自动选中 #0 并立即加载预览**（程序化设值触发 change，正好被选中即试听利用） |
| 下拉选中候选 | **立即切出该片段加载到预览播放器**；主素材与候选列表不动 |
| 📤 导出该片段 | 只写预览播放器 + 提示文件路径；**不碰主素材** |
| 设为主素材 | 唯一会替换主素材的操作（显式意图）：换素材 → 顺手体检 → 候选作废 |

实现要点：`seg_pick.change` → `on_seg_preview`（复用 `_export_segment`，
切片永远按「扫描时那个音频」的偏移）；清空选择触发的空 change 静默忽略，
不再像旧版那样弹错误提示。

### 二、下载按钮

Gradio 5.45 `gr.Audio(show_download_button=...)` 默认是 **None**（主题继承，
实测不显示）—— 这就是「音频栏没有下载」的根因。全部 14 处音频组件显式
开启：工厂（widgets.make_component，覆盖合成页音色/情感/记忆恢复的音频）+
 合成结果 / 批量全局参考与试听 / 预设表单两处 / 工作台五处。

### 验收

| ID | 内容 | 状态 | 验收证据 |
|---|---|---|---|
| l1 | 选中即试听 / 试听导出不替换主素材 / 显式替换才作废候选 | `[x]` | `tools/lab_ui_acceptance.py` **18/18**（自造 40s 合成语音 + 自起服务器 + 真浏览器；主素材 href 全程比对） |
| l2 | 全部音频栏带下载按钮 | `[x]` | 同上（输入侧 + 输出侧 DOM 断言 `a[download]` 存在且指向当前文件）；build_check / ui_output_check / lab_probe 57 项全过 |

**验收脚本本身踩的三个坑**（都固化成注释了）：切 Tab 必须卡
`role="tab"` 且真实高度（Gradio 的 Tab 栏在 DOM 里有 1px 隐藏副本，点到
死元素页面不会切换）；gr.Audio 的 `<audio>` 元素是懒创建的，读 src 拿到
null —— 用下载链接 `a[download]` 当值探针（上传型组件的 download 属性是
字面量 'true'，真实路径在 href 里）；下拉选中项带 `✓ ` 前缀，匹配用
`includes('#N ')`（后跟空格）而不是行首锚定。
